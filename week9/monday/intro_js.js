//MDN, Tutorials point, Freecodeamp
//any thing you do on then website is an event, you can listen to it and do something when it happens
//diffrence bte a closing tag and an an element is that an element is a tag with content and a closing tag is just a tag that closes the element
// diffrence nte content and value is that content is what is between the opening and closing tag and value is what is assigned to an attribute
// an attribute is a property of an element that provides additional information about the element, it is always in the opening tag and it has a name and a value
// an event is an action that occurs in the browser, such as a click, a hover, a key press, etc. you can listen to these events and do something when they happen
// a function is a block of code that performs a specific task, it can be called and executed when needed
// a variable is a container for storing data, it has a name and a value, you can change the value of a variable at any time
//diffremce bte  extermal amd intrenal js is that internal js is written inside the html file using the script tag and external js is written in a separate file with a .js extension and linked to the html file using the script tag with the src attribute
let age = 23;
function years() {
   return `Hi i am ${age} years old`;
};
years();

var p = 2;
const p = 3; // this will cause an error because you cannot reassign a value to a constant variable, this only works within scope.
//learn datatype conversion, type coercion, and type checking
//what is bulk bounty?
//what is a live server?